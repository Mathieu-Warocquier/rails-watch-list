<h1>List</h1>

<% @lists.each do |list| %>
  <p><%= list.name %></p>
  <p>
    <%= link_to "Show this list", list_path(list), class: "btn btn-success" %>
  </p>
<% end %>


<%= link_to "Create a movie list", new_list_path, class: "btn btn-primary" %>
